To run the python scripts you need first to install the librairies in requirements.txt
app.ipynb file contains commented cells which allows you to understand the code.
to run the app run the terminal in the current directory and run : python app.py 

